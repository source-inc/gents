{
  "provenance": {
    "grok_build_sha": "bc7f02eddd3d84085849dc19ed216f11c23b0571",
    "source": "audited live recon trace from run grok-port-20260830T031625Z-98089",
    "note": "Replace each historical surface_id prefix with the current run correlation; preserve the 13 audited packets and quoted evidence."
  },
  "surfaces": [
    {
      "area": "tool_call",
      "evidence": "\"let is_completed = matches!(tc.status, acp::ToolCallStatus::Completed | acp::ToolCallStatus::Failed);\"; \"self.suppressed_tools.insert(tc.tool_call_id.0.to_string());\"; \"pending.base.update(tcu.fields);\"; \"Some(meta) => serde_json::from_value::<CanonicalToolMeta>(meta.clone()).is_ok_and(|meta| { meta.version == TOOL_META_VERSION && meta.kind == ToolKind::ActiveAgentMessage })\"; \"if is_task_tool(&tc) { let is_background = tc.meta.as_ref().and_then(|m| m.get(\\\"subagentBackground\\\")).and_then(serde_json::Value::as_bool);\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentToolCall` (`AgentToolCallRow` in `row.rs`) — tool_call_id, tool_name, args, result, status, and authoritative lifecycle_state. `AgentToolResult` remains the separate conversation audit row when the runtime writes one; the shim projects by request_id from AgentToolCall. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/tracker.rs (`handle_tool_call`, `handle_tool_call_update`, `handle_update`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/subagent_message.rs (`is_tool`, `to_block`)",
      "grok_wire": "Notification \"session/update\" variants: {\"sessionUpdate\":\"tool_call\",\"toolCallId\":\"<id>\",\"title\":\"<string>\",\"kind\":\"read\"|\"edit\"|\"delete\"|\"move\"|\"search\"|\"execute\"|\"think\"|\"fetch\"|\"other\",\"status\":\"pending\"|\"in_progress\"|\"completed\"|\"failed\",\"content\":[...],\"rawInput\":{...},\"rawOutput\":{...},\"_meta\"?:{...}} and flattened {\"sessionUpdate\":\"tool_call_update\",\"toolCallId\":\"<id>\",\"status\":\"completed\",\"rawOutput\":{...}}; there is no fields wrapper. When canonical tool metadata is present its exact key is x.ai/tool. Bash output deltas stream through rawOutput with incremental UTF-8 decoding. Gents suppresses todo, background-plumbing, goal, scheduler, and workflow families; task remains on the standard ACP rail with an explicit _meta.subagentBackground boolean, and the pager owns task suppression plus foreground-wait bookkeeping. send_subagent_message is recognized by canonical _meta key x.ai/tool or title, with rawInput {\"subagent_id\",\"text\"}. available_commands_update carries availableCommands plus _meta.tools.",
      "live_expect": "Grok-wire: at least one tool_call is emitted with stable non-empty toolCallId, mapped kind, structured rawInput, optional `_meta` (using the exact canonical `x.ai/tool` key when canonical metadata is present), and the authoritative observed lifecycle status; task calls remain on the standard rail and carry an explicit `_meta.subagentBackground` boolean. A later lifecycle change emits a flattened tool_call_update with no fields wrapper. available_commands_update carries availableCommands plus `_meta.tools`. A fast call may first be observed already completed. Gents-document: one AgentToolCall per toolCallId reaches terminal lifecycle_state with args/result preserved.",
      "live_prompt": "List the files in the repository root, then read the first five lines of README.md.",
      "surface_id": "grok-port-20260830T031625Z-98089:tool_call:tracker-stream",
      "verdict": "implement"
    },
    {
      "area": "model",
      "evidence": "\"meta.get(\\\"totalContextTokens\\\").and_then(|value| match value { serde_json::Value::Number(number) => number.as_u64(), _ => None })\"; \"let req = acp::SetSessionModelRequest::new(session_id, model_id.clone()).meta(meta);\"; \"m.insert(REASONING_EFFORT_META_KEY.to_string(), reasoning_effort_meta_value(eff));\"; \"pub fn current_model_name(&self) -> Option<String> { let current = self.current.as_ref()?; if let Some(model_info) = self.available.get(current) { Some(model_info.name.clone()) } else { Some(current.0.to_string()) } }\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentBehavior` selects model_name and backend_id; `InferenceBackend`/`InferenceProfile` own endpoint and context configuration; `AgentRequest` records the resolved backend_id. AgentSession has no model or context-window fields. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/model_state.rs (`ModelState`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::SwitchModel`)",
      "grok_wire": "session/new result models: {\"availableModels\":[{\"modelId\":\"<id>\",\"name\":\"<display>\",\"meta\":{\"totalContextTokens\":u64,\"acceptsImages\":false,\"inputModalities\":[\"text\"],\"supportsReasoningEffort\":false}}],\"currentModelId\":\"<id>\"}. The Grok wire can represent image and reasoning capabilities, but this adapter must advertise only what reaches provider inference. Request \"session/set_model\" params: {\"sessionId\":\"<id>\",\"modelId\":\"<id>\"} → empty result; supplying _meta.reasoningEffort is invalid params while unsupported. Ext notification \"x.ai/models/update\" params are the flat model catalog object, not a nested models wrapper. Display name falls back to modelId when absent.",
      "live_expect": "Grok-wire: models.availableModels contains GLM-5.3-Flash-NVFP4 as currentModelId with totalContextTokens=524288, acceptsImages=false, inputModalities=[text], supportsReasoningEffort=false, and no reasoningEfforts list; session/set_model to the same id without reasoningEffort succeeds and emits x.ai/models/update with flat catalog params and no nested models wrapper. Gents-document: the serving AgentBehavior/InferenceBackend select GLM-5.3-Flash-NVFP4 and the prompt AgentRequest records that backend.",
      "live_prompt": "Name the exact model identifier you are running as, verbatim, nothing else.",
      "surface_id": "grok-port-20260830T031625Z-98089:model:catalog-switch",
      "verdict": "implement"
    },
    {
      "area": "tool_call",
      "evidence": "\"acp_define_request_response!(acp::RequestPermissionRequest, acp::RequestPermissionResponse, acp::CLIENT_METHOD_NAMES.session_request_permission);\"; \"AcpClientMessage::RequestPermission(perm) => handle_permission_request(perm, app)\"; \"meta.insert(\\\"yoloMode\\\".into(), serde_json::Value::Bool(mode.is_always_approve())); meta.insert(\\\"autoMode\\\".into(), serde_json::Value::Bool(mode.is_auto()));\"; \"/// Auto-approve all tool executions without confirmation (YOLO mode). When true, the leader will inject `yoloMode: true` into session/new requests.\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentToolApproval` collection exists in crates/gents-schemas but stays unwritten under yolo/auto modes; no Grok permission UI is ported and no DefraDB access-control policy is added. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-acp-lib/src/message.rs (`RequestPermissionRequest`, `RequestPermissionResponse`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/acp_handler/mod.rs (`AcpClientMessage::RequestPermission`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`apply_permission_mode_override`); xai-org/grok-build/crates/codegen/xai-grok-shell/src/leader/protocol.rs (`ClientCapabilities::yolo_mode`)",
      "grok_wire": "Client-side ACP request \"session/request_permission\" (RequestPermissionRequest → RequestPermissionResponse) exists in the protocol surface and is routed to the client when the agent needs approval. It is suppressed when session/new|session/load _meta carries yoloMode:true (approve-all) or autoMode:true (classifier auto); the leader injects these from client capabilities, and the pager's apply_permission_mode_override stamps the same keys. A live chat turn under yoloMode therefore never issues session/request_permission.",
      "live_expect": "Grok-wire: with _meta.yoloMode=true on session/new, the file-write tool call runs to completed with NO session/request_permission request observed on the wire. Gents-document: AgentToolCall/AgentToolResult documents show the completed write; zero AgentToolApproval documents are created.",
      "live_prompt": "Create a file named gents-permission-probe.txt containing exactly the word ok.",
      "surface_id": "grok-port-20260830T031625Z-98089:tool_call:permission-gate",
      "verdict": "ignore"
    },
    {
      "area": "session",
      "evidence": "\"let result = helpers::acp_send_bounded(acp::NewSessionRequest::new(session_cwd.clone()).mcp_servers(mcp_servers).meta(meta), &tx, \\\"Session creation\\\").await;\"; \"meta.get_or_insert_with(acp::Meta::new).insert(\\\"modelId\\\".into(), serde_json::json!(mid.0));\"; \"acp::LoadSessionRequest::new(acp_session_id.clone(), cwd.clone()).mcp_servers(mcp_servers.clone()).meta(meta.clone())\"; \"The stamp is `_meta.isReplay`, set by xai-grok-shell's `forward_raw_replay_line` during `session/load`.\"; \"meta.get_or_insert_with(acp::Meta::new).insert(\\\"x.ai/restore_code\\\".into(), serde_json::Value::Bool(rc));\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentSession` (`AgentSessionRow` in `row.rs`) records session_id, agent/behavior identity, timestamps, and status. It has no cwd or model fields. AgentConversation and AgentRequest are created by normal request execution, not by a fresh session/new. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::CreateSession`, `Effect::LoadSession`); xai-org/grok-build/crates/codegen/xai-acp-lib/src/message.rs (`agent_method_names`)",
      "grok_wire": "JSON-RPC 2.0 over the ACP stream. Request \"initialize\" (acp::AGENT_METHOD_NAMES.initialize) first; each advertised authMethods entry includes both id and human-readable name. Request \"session/new\" params: {\"cwd\":\"<abs path>\",\"mcpServers\":[],\"_meta\":{\"yoloMode\":bool,\"autoMode\":bool,\"modelId\":\"<id>\",\"sessionId\":\"<preferred id>\"}} → result {\"sessionId\":\"<id>\",\"models\":{\"availableModels\":[{\"modelId\":\"...\",\"name\":\"...\",\"meta\":{...}}],\"currentModelId\":\"...\"},\"_meta\":{...}}. Request \"session/load\" params: {\"sessionId\":\"<id>\",\"cwd\":\"<abs path>\",\"mcpServers\":[],\"_meta\":{\"x.ai/restore_code\":bool,\"cursor\":\"<eventId>\"}} → during load the agent replays persisted session/update notifications each stamped _meta.isReplay=true, then the result carries restore summary meta and the running promptId. Request \"session/set_mode\" exists (session_set_mode). Notifications \"session/update\" carry {\"sessionId\",\"update\",...} with _meta {promptId,totalTokens,eventId:\"{sessionId}-{counter}\",isReplay}.",
      "live_expect": "Grok-wire: initialize advertises loadSession=false and every authMethods entry includes id and nonempty name; session/new honors the preferred sessionId and returns GLM-5.3-Flash-NVFP4 as currentModelId; session/set_mode emits current_mode_update with currentModeId; session/load returns JSON-RPC method-not-found instead of fabricating replay. Gents-document: exactly one AgentSession exists for the returned id and zero AgentRequest rows exist before the first prompt.",
      "live_prompt": "Reply with exactly: session ready",
      "surface_id": "grok-port-20260830T031625Z-98089:session:new-load",
      "verdict": "implement"
    },
    {
      "area": "session",
      "evidence": "\"let prompt = vec![plain_prompt_content_block(text, &skill_token_ranges)]; let req = acp::PromptRequest::new(session_id.clone(), prompt).meta(prompt_request_meta(&prompt_id, screen_mode).as_object().cloned());\"; \"map.insert(\\\"promptId\\\".into(), serde_json::Value::String(prompt_id.into()));\"; \"if send_now && let Some(map) = meta.as_object_mut() { map.insert(\\\"sendNow\\\".into(), serde_json::Value::Bool(true)); }\"; \"/// Build the `PromptRequest._meta` payload: `promptId` for notification / response correlation, plus `screenMode`.\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentRequest` (`AgentRequestRow` in `row.rs`) stores prompt text and JSON metadata containing promptId/screenMode/sendNow; `AgentResponse` (`AgentResponseRow` in `row.rs`) stores status, token_count, completed_at, and interrupted_at. stopReason is an adapter projection, not a persisted field. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::SendPrompt`, `SendPromptBlocks`, `SendPromptNow`, `SendBashCommand`, `prompt_request_meta`)",
      "grok_wire": "Request \"session/prompt\" params: {\"sessionId\":\"<id>\",\"prompt\":[{\"type\":\"text\",\"text\":\"<user text>\",\"meta\":{\"skillTokenRanges\":[[start,end],...]}}],\"_meta\":{\"promptId\":\"<uuid>\",\"screenMode\":\"fullscreen\"|\"inline\"|\"minimal\",\"sendNow\":true?}} → response {\"stopReason\":\"end_turn\"|...}. The bash variant stamps the text block meta with PromptBlockMeta::bash(command) instead of skillTokenRanges. The agent echoes _meta.promptId on every session/update emitted while processing that prompt; user echo returns as a session/update \"user_message_chunk\" with block meta promptIndex / hideFromScrollback.",
      "live_expect": "Grok-wire: session/prompt carries _meta.promptId; every session/update for the turn repeats it; user_message_chunk precedes assistant output; the response resolves stopReason=end_turn. Gents-document: one AgentRequest has the prompt text and promptId in metadata, and one AgentResponse reaches status=complete with completed_at set.",
      "live_prompt": "In one sentence, what is Gents?",
      "surface_id": "grok-port-20260830T031625Z-98089:session:prompt-turn",
      "verdict": "implement"
    },
    {
      "area": "attach",
      "evidence": "\"let len = u32::from_be_bytes(len_buf); if len > MAX_MESSAGE_SIZE { return Err(ProtocolError::MessageTooLarge(len)); }\"; \"pub const LEADER_PROTOCOL_VERSION: u32 = 1;\"; \"/// When true, the leader will inject `yoloMode: true` into session/new requests.\"; \"Registered { client_id: u64, /// Whether the leader is fully initialised and ready to forward ACP traffic. #[serde(default = \\\"default_ready\\\")] ready: bool\"; \"Acp { payload: String }\"; \"fn wire_name(self) -> String { format!(\\\"_{}\\\", self.name()) }\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentSession` records the ACP session id after session/new. Client capability flags are connection-local shim state and are not fields on AgentSession. No permission request or access-control document is fabricated. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-shell/src/leader/protocol.rs (read in full); xai-org/grok-build/crates/codegen/xai-acp-lib/src/message.rs (read in full)",
      "grok_wire": "Transport: 4-byte big-endian u32 length header then one JSON value per frame; MAX_MESSAGE_SIZE = 64MB; EOF => ConnectionClosed. Client→Server envelopes (tag \"type\", snake_case): {\"type\":\"register\",\"client_type\":\"<string>\",\"mode\":\"stdio\"|\"headless\",\"capabilities\":{\"yolo_mode\":bool,\"auto_mode\":bool,\"default_model\":string|null,\"client_version\":string|null,\"code_nav_enabled\":bool,\"terminal\":bool,\"fs_read\":bool,\"fs_write\":bool,\"status_line\":bool}}; {\"type\":\"acp\",\"payload\":\"<one JSON-RPC 2.0 line>\"}; {\"type\":\"control\",\"request_id\":\"<string>\",\"command\":{...}}; {\"type\":\"ping\"}; {\"type\":\"disconnect\"}. Server→Client: {\"type\":\"registered\",\"client_id\":u64,\"ready\":bool,\"leader_protocol_version\":1,\"leader_binary_version\":\"...\",\"leader_capabilities\":{\"control_v1\":bool,\"runtime_cpu_profile\":bool,\"profile_formats\":[\"svg\"|\"folded\"],\"workspace_exposure\":bool,\"relaunch_v1\":bool}}; if ready=false the client MUST wait for {\"type\":\"leader_ready\"} before any ACP traffic; {\"type\":\"acp\",\"payload\":\"...\"} both directions; {\"type\":\"shutting_down\",\"reason\":\"auto_update\"|\"manual\"|\"idle_timeout\",\"delay_ms\":0} then {\"type\":\"shutdown\"}; {\"type\":\"pong\"}; {\"type\":\"error\",\"code\":i32,\"message\":\"...\"}. Internal agent methods are wire-named with a leading underscore so the decoder routes them to ext_method/ext_notification: \"_x.ai/internal/auth_cleared\", \"_x.ai/internal/evict_sessions\", \"_x.ai/internal/reload_all_mcp_servers\", \"_x.ai/internal/reload_models\", \"_x.ai/internal/reload_models_cache\", \"_x.ai/internal/reload_project_mcp_servers\", \"_x.ai/internal/reload_skills\", \"_x.ai/internal/reload_workflows\".",
      "live_expect": "Grok-wire: register is answered by registered{ready:true,leader_protocol_version:1}; ping returns pong; subsequent ACP frames carry initialize/authenticate/session-new JSON-RPC. The sibling leader lock remains exclusively owned so stock Grok cannot replace the shim. Gents-document: one AgentSession exists with the returned ACP sessionId; no capability or ACL document is fabricated.",
      "live_prompt": "hello — reply with exactly one word.",
      "surface_id": "grok-port-20260830T031625Z-98089:attach:leader-register",
      "verdict": "implement"
    },
    {
      "area": "interrupt",
      "evidence": "\"let mut params = serde_json::json!({ \\\"sessionId\\\": session_id.0.to_string(), \\\"text\\\": text, \\\"interjectionId\\\": interjectionId, });\"; \"let req = acp::ExtRequest::new(\\\"x.ai/interject\\\", serde_json::value::to_raw_value(&params).expect(\\\"serialize interject params\\\").into());\"; \"\\\"x.ai/session/interjection\\\" => handle_interjection(notif, app)\"; \"if let Some(iid) = interjectionId { if agent.self_interjection_ids.remove(iid) { return false; }\"",
      "expected_total": "13",
      "gents_docs": "No Gents document is written for interjection because the owned completion loop has no formally specified injection transition. Writing a detached AgentMessage would not affect provider input and is forbidden.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::SendInterject`, `build_interject_params`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/acp_handler/mod.rs (`handle_interjection`)",
      "grok_wire": "Ext request \"x.ai/interject\" params: {\"sessionId\":\"<id>\",\"text\":\"<user text>\",\"interjectionId\":\"<uuid>\",\"content\":[ContentBlock...]?} — the optional structured content key is omitted entirely when absent. The agent then emits ext notification \"x.ai/session/interjection\" params {\"sessionId\":\"<id>\",\"text\":\"<text>\",\"interjectionId\":\"<id>\"}; the originating client dedups its own interjection by interjectionId (self_interjection_ids) and renders it as an interjection prompt block rather than a new turn. Queue-based interjection (\"x.ai/queue/interject\") is queue chrome and out of scope unless a live turn requires it.",
      "live_expect": "Grok-wire: x.ai/interject returns JSON-RPC method-not-found with an explicit formal-transition explanation; no x.ai/session/interjection success notification is emitted. Gents-document: zero AgentMessage or AgentRequest documents are fabricated by the rejected request.",
      "live_prompt": "Write a detailed comparison of CRDTs and operational transforms. (mid-turn, interject: \"Just give me the one-line takeaway.\")",
      "surface_id": "grok-port-20260830T031625Z-98089:interrupt:interject",
      "verdict": "shaped-stub"
    },
    {
      "area": "subprocess",
      "evidence": "\"AcpClientMessage::WaitForTerminalExit(args) => { args.response_tx.send(Err(crate::acp::wait_for_exit_not_supported(\\\"pager\\\"))).ok(); false }\"; \"/// When true, the leader injects `clientTerminal: true` into `session/new` and `session/load` so the agent routes terminal commands to the client via ACP instead of running them locally.\"; \"let req = acp::ExtRequest::new(\\\"x.ai/terminal/background\\\", serde_json::value::to_raw_value(&params).expect(\\\"serialize background params\\\").into());\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentToolCall` (execute kind) is the authoritative subprocess record and preserves args plus inline result. `AgentToolResult` is an optional spill/audit row only when the runtime writes one; no terminal documents are introduced. Typed graph writes only; no DefraDB access-control policy.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/acp_handler/mod.rs (`AcpClientMessage::WaitForTerminalExit`); xai-org/grok-build/crates/codegen/xai-grok-shell/src/leader/protocol.rs (`ClientCapabilities::terminal`); xai-org/grok-build/crates/codegen/xai-acp-lib/src/message.rs (terminal client method types); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`x.ai/terminal/background` request construction)",
      "grok_wire": "Client-side ACP methods exist: \"terminal/create\" (CreateTerminalRequest→CreateTerminalResponse), \"terminal/output\", \"terminal/release\", \"terminal/wait_for_exit\", \"terminal/kill\". They are only routed to the client when the leader injected _meta clientTerminal:true into session/new|session/load, which happens only when the registering client set capabilities.terminal=true. The reference pager registers terminal=false and answers terminal/wait_for_exit with a not-supported error. Backgrounding a running command is the ext request \"x.ai/terminal/background\" params {\"sessionId\":\"<id>\",\"terminalId\":\"<tool_call_id>\"}. Shell work otherwise runs agent-side and reaches the client purely as execute-kind tool_call/tool_call_update events.",
      "live_expect": "Grok-wire: the turn produces an execute tool_call with rawInput.command \"echo gents-subprocess-probe\" and rawOutput containing that string; NO terminal/create or terminal/output request is sent to the client (clientTerminal false). Gents-document: AgentToolCall args/result capture the command and output; if the runtime spilled the result, the linked AgentToolResult also matches, but a spill row is not required for an inline result. Stub behavior: if any terminal/* request arrives, reply with the pager-style not-supported error instead of implementing a terminal.",
      "live_prompt": "Run the shell command `echo gents-subprocess-probe` and show its output.",
      "surface_id": "grok-port-20260830T031625Z-98089:subprocess:terminal-acp",
      "verdict": "shaped-stub"
    },
    {
      "area": "interrupt",
      "evidence": "\"let req = acp::CancelNotification::new(session_id.clone()).meta(meta.as_object().cloned());\"; \"let mut meta = serde_json::json!({ \\\"cancelSubagents\\\": cancel_subagents });\"; \"meta[\\\"rewindIfNoOutput\\\"] = true.into(); meta[\\\"rewindIfPristine\\\"] = true.into(); meta[\\\"promptId\\\"] = pid.into();\"; \"pub fn finish_turn(&mut self, scrollback: &mut ScrollbackState) { ... self.blocking_waits.clear(); self.orphan_updates.clear();\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: AgentRequest is stamped interrupt_requested_at and reaches interrupted lifecycle state; AgentResponse keeps its normal status vocabulary and is stamped interrupted_at as the durable cancellation marker. Runtime child AgentRequest rows, not static Task configuration rows, are cancellation targets. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::CancelTurn`); xai-org/grok-build/crates/codegen/xai-acp-lib/src/message.rs (`CancelNotification`, `session_cancel`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/tracker.rs (`finish_turn`)",
      "grok_wire": "Notification \"session/cancel\" params: {\"sessionId\":\"<id>\",\"_meta\":{\"cancelSubagents\":bool,\"cancelTrigger\":\"<wire string>\"?,\"rewindIfNoOutput\":bool,\"rewindIfPristine\":bool,\"promptId\":\"<id>\"}}. It is a notification (no response body). The turn then terminates via the pending session/prompt response; the client's tracker clears blocking waits, suppressed tools, orphan updates, and pending tool entries on finish_turn. cancelSubagents=true also cancels live subagents of the session.",
      "live_expect": "Grok-wire: session/cancel notification with the same promptId and cancelSubagents=true stops the turn; the outstanding session/prompt resolves stopReason=cancelled. Gents-document: AgentRequest.interrupt_requested_at and AgentResponse.interrupted_at are non-null; no static Task row is mutated.",
      "live_prompt": "Write a 1500-word essay on the history of database systems. (cancel the turn after the first streamed paragraph)",
      "surface_id": "grok-port-20260830T031625Z-98089:interrupt:cancel",
      "verdict": "implement"
    },
    {
      "area": "context",
      "evidence": "\"pub total_tokens: Option<u64>,\"; \"pub fn note_context_used(&mut self, used: u64)\"; \"pub fn get_context_window(&self) -> Option<u64> { self.context_window_override.or_else(|| self.current_context_window_tokens()) }\"; \"if let Some(tokens) = meta.total_tokens { confirm_context_used(agent, tokens); }\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: AgentResponse.token_count is the persisted source projected into connection-local cumulative totalTokens metadata. AgentSession has no usage counter. ProviderContextReduction/RenderedRequest remain runtime-owned local-audit collections and are never fabricated by the shim.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/meta.rs (`NotificationMeta`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/tracker.rs (`note_context_used`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/model_state.rs (`get_context_window`)",
      "grok_wire": "Every standard-rail \"session/update\" notification carries _meta.totalTokens (u64, cumulative session tokens) which the client confirms into its context-used state; the window comes from the current model's meta key \"totalContextTokens\" (u64). Extension-rail notifications (\"x.ai/session_notification\" and its replay alias) track applied events per rail and may omit totalTokens, and transient subagent progress may omit metadata entirely, so per-rail high-water semantics are enforced separately. For subagent views the window is overridden by SubagentProgress.context_window_tokens. Usage percent = totalTokens / totalContextTokens. No client→agent context message exists; the client is a pure consumer of these _meta fields.",
      "live_expect": "Grok-wire: every session/update has a u64 _meta.totalTokens that never decreases, and the value is bounded by totalContextTokens=524288. Gents-document: the terminal AgentResponse has token_count; no AgentSession usage field or synthetic ProviderContextReduction is written.",
      "live_prompt": "Write a 300-word paragraph explaining what a context window is.",
      "surface_id": "grok-port-20260830T031625Z-98089:context:token-meta",
      "verdict": "implement"
    },
    {
      "area": "session",
      "evidence": "\"acp::SessionUpdate::AgentMessageChunk(chunk) => { self.blocking_waits.clear(); self.handle_agent_chunk(chunk, meta, scrollback) }\"; \"total_tokens: m.get(\\\"totalTokens\\\").and_then(|v| v.as_u64()),\"; \"pub const PROMPT_INDEX: &str = \\\"promptIndex\\\";\"; \"/// Monotonic per-process sequence parsed from `eventId` (`\\\"{sessionId}-{counter}\\\"` ...).\"; \"if let Some(seq) = meta.event_seq && !meta.is_replay && !dedup_drop { agent.last_applied_event_seq = Some(seq); }\"",
      "expected_total": "13",
      "gents_docs": "Gents documents: `AgentMessage` (`AgentMessageRow` in `row.rs`) — one document per streamed message entry, text accumulated from chunks; `AgentResponse` links the turn. Typed graph writes only.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/tracker.rs (`handle_update`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/meta.rs (`NotificationMeta`, `from_json`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/acp_handler/mod.rs (`SessionNotification` routing)",
      "grok_wire": "Notification \"session/update\" params: {\"sessionId\":\"<id>\",\"update\":{\"sessionUpdate\":\"agent_message_chunk\",\"content\":{\"type\":\"text\",\"text\":\"<delta>\"}},\"_meta\":{...}}. The chunk field is content, not contentBlock. current_mode_update carries currentModeId. available_commands_update carries availableCommands and _meta.tools. _meta includes totalTokens, promptId, isReplay, and eventId \"{sessionId}-{counter}\"; non-replay counters deduplicate per rail.",
      "live_expect": "Grok-wire: one or more agent_message_chunk updates use update.content, share the promptId, have monotonic eventId counters/nondecreasing totalTokens, and follow a user_message_chunk echo; current_mode_update uses currentModeId and available_commands_update uses availableCommands plus `_meta.tools`. Gents-document: the terminal assistant AgentMessage content equals the concatenated projected text with no duplicated final overlay.",
      "live_prompt": "Count from one to five, one number per line.",
      "surface_id": "grok-port-20260830T031625Z-98089:session:stream-chunks",
      "verdict": "implement"
    },
    {
      "area": "context",
      "evidence": "\"let req = acp::ExtRequest::new(\\\"x.ai/compact_conversation\\\", serde_json::value::to_raw_value(&params).expect(\\\"serialize compact params\\\").into());\"; \"scrollback.push_block(RenderBlock::session_event(SessionEvent::CompactionCompleted { tokens_before: pending.tokens_before, tokens_after: pending.last_used.unwrap_or(pending.estimate_after), elapsed_ms: pending.elapsed_ms }));\"; \"/// This is **persist-only** — it is never sent to the gateway/UI.\"",
      "expected_total": "13",
      "gents_docs": "No CompactionEntry is written by this adapter request. CompactionEntry is runtime-owned and its schema does not contain the invented tokens_before/tokens_after fields; AgentSession has no usage counters.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::Compact`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/acp/tracker.rs (`finish_turn`, `PendingCompaction`); xai-org/grok-build/crates/codegen/xai-grok-shell/src/extensions/notification.rs (`CompactionCheckpoint`)",
      "grok_wire": "Ext request \"x.ai/compact_conversation\" params: {\"sessionId\":\"<id>\"} → ext response result (error string on failure). Auto-compaction mid-turn surfaces as tracker activity AutoCompacting (set from ExtNotification, not a standard SessionUpdate). On the wire the durable marker is a CompactionCheckpoint update persisted to updates.jsonl only (never sent to the UI); the UI-visible signal is the turn finishing with a compaction-completed event carrying tokens_before, tokens_after, elapsed_ms. Subsequent session/update _meta.totalTokens restart from the compacted baseline.",
      "live_expect": "Grok-wire: x.ai/compact_conversation returns JSON-RPC method-not-found with an explicit owned-transition explanation. Gents-document: no CompactionEntry or AgentSession field is fabricated by the rejected request.",
      "live_prompt": "/compact",
      "surface_id": "grok-port-20260830T031625Z-98089:context:compaction",
      "verdict": "shaped-stub"
    },
    {
      "area": "subagent",
      "evidence": "\"SubagentSpawned { subagent_id: String, parent_session_id: String, ... child_session_id: String, subagent_type: String, description: String\"; \"SubagentProgress { ... tokens_used: u64, context_window_tokens: u64, context_usage_pct: u8, tools_used: Vec<String>, error_count: u32\"; \"pub struct CancelSubagentRequest { pub subagent_id: String }\"; \"\\\"x.ai/subagent/cancel\\\" => {\"; \"Some(SessionMatch::Child(parent_id)) => { ... child_view.session.handle_update(notif.request.update, &meta, &mut child_view.scrollback);\"",
      "expected_total": "13",
      "gents_docs": "Runtime subagents are child AgentRequest rows linked by caused_by_parent_request_id; Task is static automation configuration and is never treated as runtime status. The shim can project observed child spawn/finish rows, but the ext get/list/cancel controls are shaped not-found stubs in v1.",
      "grok_call_sites": "xai-org/grok-build/crates/codegen/xai-grok-shell/src/extensions/notification.rs (`SubagentSpawned`, `SubagentProgress`, `SubagentFinished`); xai-org/grok-build/crates/codegen/xai-grok-shell/src/extensions/task.rs (`CancelSubagentRequest`, `SubagentCancelOutcomeDto`, `CancelSubagentResponse`, request handlers); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/effects/mod.rs (`Effect::KillSubagent`); xai-org/grok-build/crates/codegen/xai-grok-pager/src/app/acp_handler/mod.rs (`SessionMatch::Child`)",
      "grok_wire": "On the PARENT session's channel via the extension-rail method \"x.ai/session_notification\" (the exact live method; \"x.ai/session/update\" is its replay alias only). The outer SessionNotification envelope is camelCase (\"sessionId\",\"update\",\"_meta\"), while the tagged SessionUpdate enum is #[serde(rename_all=\"snake_case\", tag=\"sessionUpdate\")] — the rename applies to the tag and variant names only, so every variant FIELD serializes snake_case. Spawned: {\"sessionUpdate\":\"subagent_spawned\",\"subagent_id\":\"<id>\",\"parent_session_id\":\"<id>\",\"parent_prompt_id\":\"<id>\"?,\"child_session_id\":\"<id>\",\"subagent_type\":\"general-purpose\"|\"explore\"|\"plan\"|custom,\"description\":\"<short>\",\"effective_context_source\":\"new\"|\"resumed\"?,\"context_normalized\":bool,\"capability_mode\":\"read-only\"?,\"persona\"?,\"role\"?,\"model\"?,\"resumed_from\"?,\"workflow_run_id\"?}. Then ~2s cadence: {\"sessionUpdate\":\"subagent_progress\",\"subagent_id\",\"parent_session_id\",\"child_session_id\",\"duration_ms\":u64,\"turn_count\":u32,\"tool_call_count\":u32,\"tokens_used\":u64,\"context_window_tokens\":u64,\"context_usage_pct\":u8,\"tools_used\":[\"...\"],\"error_count\":u32}.",
      "grok_wire_continuation": " Terminal: {\"sessionUpdate\":\"subagent_finished\",\"subagent_id\",\"child_session_id\",\"status\":\"completed\"|\"failed\"|\"cancelled\",\"error\"? (optional, skipped when None),\"tool_calls\":u32,\"turns\":u32,\"duration_ms\":u64,\"tokens_used\":u64 (required on the live wire; only old pre-tokens_used JSONL replays deserialize it as default 0),\"output\"? (optional, present when completed),\"will_wake\":bool (required; false for a foreground child)} — the exact live finished field set, which never carries parent_session_id on the live rail and carries subagent_id == child_session_id. Child session updates arrive on the parent channel and are routed by child_session_id. Ext request \"x.ai/subagent/cancel\" params {\"sessionId\":\"<id>\",\"subagentId\":\"<id>\"} → result {\"subagentId\":\"<id>\",\"cancelled\":bool,\"outcome\":{\"kind\":\"cancelled\"|\"already_finished\"|\"not_found\",\"status\"?}}; also \"x.ai/subagent/get\" {\"sessionId\",\"subagentId\"} → {\"snapshot\":null|{...}} and \"x.ai/subagent/list_running\" {\"sessionId\"} → {\"subagents\":[]} — these client request/result DTOs stay camelCase (separately audited shapes), not mechanically snake_cased.",
      "live_expect": "Grok-wire: x.ai/subagent/get for a missing id returns the generated GetSubagentResponse shape {\"snapshot\":null} (no outcome wrapper), x.ai/subagent/cancel for a missing id returns {\"subagentId\":\"<id>\",\"cancelled\":false,\"outcome\":{\"kind\":\"not_found\"}}, and list_running returns the generated ListRunningSubagentsResponse shape {\"subagents\":[]}; seeded projection tests verify child AgentRequest rows map to subagent_spawned/subagent_finished without querying Task. Gents-document: no Task or child AgentRequest is fabricated by the stub calls.",
      "live_prompt": "Spawn a subagent to list the top-level directories of this repository and summarize each one in five words.",
      "surface_id": "grok-port-20260830T031625Z-98089:subagent:lifecycle",
      "verdict": "shaped-stub"
    }
  ]
}
