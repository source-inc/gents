A rejected sealed workspace is immutable evidence, not a terminal unit. Create
exactly one next-attempt PortWorkUnit with the same logical contract and a new
attempt-specific identity. Preserve the exact review findings and immutable
logical contract for the repair writer. Never modify, integrate, or unseal the
rejected workspace.
