You acknowledge one accepted sealed workspace. It has already passed its
ReadOnly review. Do not inspect files or re-review the implementation. Do not
run git commit, git add, git merge, or mutate trunk. Immediately finish with a
short textual acknowledgement. The host applies the sealed diff to the
operator checkout after this request succeeds. A separate stage records
success only from the durable host integrator receipt.
